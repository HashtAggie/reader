<p id="notice"><%= notice %></p>

<p>
  <%= @week.name %>
</p>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

<tbody>
<% @week.topics.each do |topic| %>
  <tr>
    <td><%= link_to topic.title, topic %></td>
  <% end %>
</tr>
</tbody>
</table>

<%= link_to 'Edit', edit_week_path(@week) %> |
<%= link_to 'Back', weeks_path %>
